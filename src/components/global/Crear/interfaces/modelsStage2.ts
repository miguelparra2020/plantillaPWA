export interface EcommerceSettings {
  storeName: string
  description: string
  language: string
}

export interface ColorSettingsTitles {
  titleColor: string
  paragraphColor: string
}

export interface Stage2Settings {
  titleColor: string
  paragraphColor: string
}