---
import {   LayoutGeneral, MyComponent, QueryProvider } from "@globals"
import { ModuelesActives } from "@util/generalConfig"
if (!ModuelesActives.Reservation) {
  return Astro.redirect('/crear');
}
import CarShopping from '@img/carShopping.png';
---

    <LayoutGeneral>
      <QueryProvider>
        <MyComponent client:idle />
      </QueryProvider>
    </LayoutGeneral>